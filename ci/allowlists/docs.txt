# ci/allowlists/docs.txt — G3 docs-example conformance allowlist (bd-ws0-guards-klz98.4)
# Format: <entry>\t<bead-id>\t<reason>  (see ci/allowlists/README.md)
# Consumed by tests/docs_conformance_test.go (runs in the Go test CI job, not
# the bare reality-guards job). Entry classes:
#   skip-budget:<file>:<n>   exact count of `<!-- ntm-docs: skip -->`-waived
#                            ntm examples in <file>; ratcheted BOTH directions
#   badge-waiver:go-version  waives the go.mod-vs-README badge assert
# docs/planning/** is excluded from the gate by design (proposals archive —
# hypothetical examples; see the test header and the G3 bead close reason).
