# ci/allowlists/config.txt — G2 config-key liveness allowlist (bd-ws0-guards-klz98.3)
# Format: <entry>	<bead-id>	<reason>  (see ci/allowlists/README.md)
# Seeded by the G2 bead from the gap register (WS6 G-list + [cass] injection keys + tmux.palette_key).
# Entry = dotted leaf-key path of the parsed Config struct (see internal/config/liveness_test.go).
# Seeded 2026-08-16 from the first full liveness walk (472 unclaimed keys at zero claims):
# G-list dead knobs carry their WS6/C10 bead. The G2 live-key claims pass (2026-08-18) registered
# RegisterReader claims for 255 live keys (see internal/*/liveness_claims.go) and re-tagged the
# 153 keys with no runtime reader to bd-6otuk (dead-knob staged removal).
# A key passes by claim: config.RegisterReader(key, realReaderFunc) from the consuming package.
# bd-6otuk disposition (2026-08-18): of the 153 dead-knob keys tagged above in
# earlier revisions, 128 were deprecated in v1.28.0 (struct fields removed;
# warn-tier in internal/config/removed_knobs.go, errors in v1.29.0), 9 gained
# real claims (the 8 all-build ensemble defaults + swarm.force_global_auth_clobber
# after its flag-clobber wiring fix), and the 16 ensemble.* keys below are live
# ONLY under -tags ensemble_experimental.
# permanent:
# Blind spot: build tags. These keys' RegisterReader claims live in
# internal/cli/liveness_claims_ensemble.go behind the ensemble_experimental
# build tag, so the untagged liveness walk cannot observe them; tagged test
# runs register the claims and these entries become inert (permanent entries
# are exempt from the stale check).
ensemble.synthesis.strategy	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.synthesis.min_confidence	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.synthesis.max_findings	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.synthesis.include_raw_outputs	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.synthesis.conflict_resolution	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.budget.synthesis	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.budget.context_pack	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.cache.ttl_minutes	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.cache.cache_dir	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.cache.max_entries	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.cache.share_across_modes	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.early_stop.enabled	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.early_stop.min_agents	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.early_stop.findings_threshold	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.early_stop.similarity_threshold	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
ensemble.early_stop.window_size	permanent	build-tag blind spot: reader tag-gated (ensemble_experimental), claimed in liveness_claims_ensemble.go
